# 核心（轻量、秒装）——行情 + 研报即刻可用
fastapi>=0.110
uvicorn[standard]>=0.29
requests
duckdb>=1.4,<2

# 全栈数据源（较重）——一致预期 / 新闻 / 公告 / K线 / 财务
# 未安装时对应端点会返回 501 + 安装提示，不影响行情/研报
akshare>=1.10
mootdx>=0.10
pandas

# TrendRadar sidecar 网关客户端（TR1-P0）不进主依赖树：
# 官方 mcp SDK 要求 httpx>=0.27，与 mootdx 的 httpx<0.26 数学冲突
# （ ResolutionImpossible 证据见 ops/trendradar/QUALIFICATION.md §client-dependency ）。
# 按本仓库重依赖约定走独立锁（Windows canonical；Linux 为 CI 兼容性探针）：
# pip install -r requirements-trendradar-windows-py312.lock.txt，
# 未安装时网关返回显式 UNAVAILABLE(client-missing)，离线测试全部用 fake transport。
