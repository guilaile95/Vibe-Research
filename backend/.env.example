# VibeResearch 后端可选配置。
# 本地自托管（默认）：loopback 单用户模式，无需 key，开箱即用。
# 但 127.0.0.1 不是鉴权机制：默认 CORS 只允许官方本地前端 Origin，
# 恶意网页无法从浏览器跨源读写你的持仓/账户/研报。
# 用法：复制为 .env，或直接 export 这些环境变量后再起服务。

# ── 安全：本地 API 访问边界（P0-SEC1） ──────────────────────────────────
# CORS 白名单，逗号分隔的 http(s)://host[:port]。不设置 = 仅允许官方本地前端：
#   http://localhost:5899 , http://127.0.0.1:5899
# 禁止设为 *（私有 API + 通配 Origin 直接拒绝启动）；空值/畸形值同样拒绝启动。
# 非本地部署时改成你的真实前端 Origin：
#   VR_ALLOW_ORIGINS=https://your-frontend-host
# VR_ALLOW_ORIGINS=

# 鉴权 key。一旦设置，所有 /api/*（除 /api/health）都需带 Authorization: Bearer <key>。
# 本地 loopback 模式留空即可（浏览器跨源访问已被 Origin 白名单挡住）。
# 非 loopback 绑定（VR_HOST 为 0.0.0.0 / LAN IP / 公网 IP）时：不设置 VR_API_KEY 直接拒绝启动。
#   VR_API_KEY=$(openssl rand -hex 32)
VR_API_KEY=

# 服务绑定地址（可选）。设置后启动时强制校验：非 loopback + 无 key → 拒绝启动。
# 不设置时以 uvicorn --host 实际绑定为准，运行时仍会拒绝「非 loopback + 无 key」的请求。
# 合法值：127.0.0.1 / ::1 / localhost（loopback）；0.0.0.0 / LAN IP / 域名（非 loopback）。
# VR_HOST=127.0.0.1

# Host 头白名单（可选，逗号分隔纯主机名，防 DNS rebinding）。
# 默认已允许 localhost / 127.0.0.1 / [::1]；非本地部署时加入后端自己的主机名：
#   VR_TRUSTED_HOSTS=your-backend-host
# VR_TRUSTED_HOSTS=

# ── 数据源（可选） ───────────────────────────────────────────────────
# 行情/研报/公告/资金面等零 key 即用；仅 iwencai 语义搜索研报需要 key：
# IWENCAI_API_KEY=
# IWENCAI_BASE_URL=https://openapi.iwencai.com

# 代理：默认不用管。东财等国内数据源**默认直连**（自动绕开你的科学上网/Clash 代理，
# 因为国内财经站走翻墙代理反而会连不上，如 push2.eastmoney.com）。
# 仅当你的机器「只能靠代理才能出网」时，才设为 1 强制走系统代理：
# VR_DATA_PROXY=1

# AI：无需在此配置。用户在网页「接入 AI」页填自己的 key（存本地浏览器），
#     或用订阅接入调本机已登录的 CLI（Claude Code / Codex / Qwen / Gemini / DeepSeek）。
