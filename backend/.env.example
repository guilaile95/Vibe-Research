# VibeResearch 后端可选配置。
# 默认是 local-private 模式：后端监听 127.0.0.1，前端使用 5899 端口，无需 API key。
# 用法：在启动 Uvicorn 前显式设置所需环境变量。

# ── Local-private runtime security ───────────────────────────────────
# CORS / browser Origin 白名单，逗号分隔的精确 http(s) Origin。
# 不设置时只允许当前正式本地前端：
#   http://localhost:5899,http://127.0.0.1:5899
# 通配符、显式空值、空 CSV 项、userinfo、路径、query、fragment 均会 fail closed。
# 非本地前端必须显式设置，例如：
#   VR_ALLOW_ORIGINS=https://your-frontend-host
# VR_ALLOW_ORIGINS=http://localhost:5899,http://127.0.0.1:5899

# 鉴权 key。一旦设置，所有 /api/*（除 /api/health）都需带 Authorization: Bearer <key>。
# loopback-only 本地模式可留空；任何非 loopback 访问在 key 为空时都会返回 503。
# 非 loopback 部署必须设置强随机值：
#   VR_API_KEY=$(openssl rand -hex 32)
VR_API_KEY=

# Host 头白名单（可选，逗号分隔的精确主机名/IP，不含协议、路径或端口）。
# 默认允许 localhost、127.0.0.1、[::1]；非 loopback 部署还必须加入实际 Host：
#   VR_TRUSTED_HOSTS=your-backend-host
# VR_TRUSTED_HOSTS=your-backend-host

# 本地 CLI 执行门（P0-SEC2，默认禁用）。即使设置，HTTP 调本机 AI CLI（provider=cli-*）
# 也要求同时满足：本开关=1、VR_API_KEY 非空、且该 provider 已证明 no-tools 纯文本能力
# （当前所有 provider 均未证明 → fail-closed 拒绝）。不设本开关时 cli-* 请求返回 403。
#   VR_ENABLE_LOCAL_CLI=1

# ── 数据源（可选） ───────────────────────────────────────────────────
# 行情/研报/公告/资金面等零 key 即用；仅 iwencai 语义搜索研报需要 key：
# IWENCAI_API_KEY=
# IWENCAI_BASE_URL=https://openapi.iwencai.com

# 代理：默认不用管。东财等国内数据源**默认直连**（自动绕开你的科学上网/Clash 代理，
# 因为国内财经站走翻墙代理反而会连不上，如 push2.eastmoney.com）。
# 仅当你的机器「只能靠代理才能出网」时，才设为 1 强制走系统代理：
# VR_DATA_PROXY=1

# AI：无需在此配置。用户在网页「接入 AI」页填自己的 key（存本地浏览器），
#     或用订阅接入调本机已登录的 CLI（Claude Code / Codex / Qwen / Gemini / DeepSeek）。
